name: Default

on:
  schedule:
    - cron: '0 10 * * *' # everyday at 10am
  push:
    branches:
      - master

jobs:
  check:
    name: Check Updates
    runs-on: ubuntu-latest
    outputs:
      gitbucket: ${{ steps.gitbucket.outputs.update }}
    steps:
      - name: Checkout
        uses: actions/checkout@master
      - name: Check Gitbucket Version
        id: gitbucket
        run: |
          update=$(bash scripts/check_updates_gh.sh gitbucket/gitbucket jqjiang/gitbucket)
          echo "::set-output name=update::$update"
  
  gitbucket:
    name: Build GitBucket
    needs: check
    runs-on: ubuntu-latest
    if: ${{ needs.check.outputs.gitbucket != 'false' }}
    env:
      GITBUCKET_VER: ${{ needs.check.outputs.gitbucket }}
    steps:
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v1
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1
      - name: Login to DockerHub
        uses: docker/login-action@v1 
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - name: Build and push
        id: docker_build
        uses: docker/build-push-action@v2
        with:
          push: true
          file: gitbucket.Dockerfile
          tags: jqjiang/gitbucket:latest,jqjiang/gitbucket:${{ env.GITBUCKET_VER }}
          build-args: |
            VERSION=${{ env.GITBUCKET_VER }}
